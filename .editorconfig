root = true

[*] # 所有文件适用
charset = utf-8 # 文件字符集
indent_style = space # 缩进风格
indent_size = 2 # 缩进大小
end_of_line = lf # 换行类型
trim_trailing_whitespace = true # 去除行首任意空白
insert_final_newline = true # 始终在末尾插入一行

[*.md] # 仅在md文档下生效
max_line_length = off
trim_trailing_whitespace = false
